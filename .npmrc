# Allow React 19 even when packages specify React 16-18
auto-install-peers=true
strict-peer-dependencies=false

# Suppress peer dependency warnings for known working combinations
legacy-peer-deps=true
